import { GameConsole } from '@/components/game-console'

export function HeroImage() {
  return (
    <section className="hero-image hero-image--console" aria-label="yukyuの携帯ゲーム機">
      <GameConsole />
    </section>
  )
}
